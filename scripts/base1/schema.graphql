# base1 graphql 涉及的 schema

# Author 表
type Author {
    id: Int!    # 该类型为 int，加上 ! 表示该字段是必需的
    nickname: String
}

# Post 表
type Post {
    id: Int!
    title: String
    votes: Int
}

# 特定的query（需要做实验确定的query的构建方法）
type Query {
    author(id: Int!): Author
}